import 'package:flutter/material.dart';

class NonAuthUser extends StatelessWidget {
  const NonAuthUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
